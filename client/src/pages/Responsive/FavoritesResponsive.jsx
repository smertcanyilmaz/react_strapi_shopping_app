import React from "react";
import CartCard from "../../components/CartCard/CartCard";
const FavoritesResponsive = () => {
  return <CartCard showPayment={false} responsive={true} />;
};

export default FavoritesResponsive;
